# Disclaimer

This is an stdlib-only portion of `genvm-tool` to invoke on CI before nix

## Requirements

1. `/bin/sh`
2. `python3` interpreter in `$PATH`
3. working `gh` CLI
4. `nix` for most commands
